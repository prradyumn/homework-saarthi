# Runtime only — what `serve.py` imports once the corpus is already built.
#
# NO torch and NO sentence-transformers. The corpus index ships precomputed; the
# only thing that needs embedding at request time is the query, and that now goes
# to Cloudflare Workers AI (`@cf/baai/bge-m3` — the same model the index was built
# with, so the shipped vectors stay valid). Carrying BGE-M3 locally cost ~1.9 GB
# of RAM and forced a 4 GB host; Hugging Face put the last free tier that size
# behind PRO in July 2026. See scripts/embedder.py and DECISIONS D19.
#
# The full requirements.txt is still the INGEST and EVAL environment, and it keeps
# torch: every eval runs the model locally, offline and reproducibly.
numpy==2.5.2
pymupdf==1.28.2
pillow==12.3.0
