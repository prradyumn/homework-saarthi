.venv/
__pycache__/
# NCERT PDFs and page renders are not redistributable ("© NCERT / not to be
# republished"). The repo ships the fetch script and derived artifacts, not the book.
ingest/raw/
ingest/pages/
ingest/extracted/
# chapter PDFs pagesource.py fetches on demand — same book, same licence
ingest/pdf_cache/

# generated by scripts/make_deploy.py, and carries its own git history for
# pushing to a Space. Regenerate it; never commit it into this tree.
deploy/
.deploy-git-stash/

# secrets — never commit
.env
