.venv/
__pycache__/
# NCERT PDFs and page renders are not redistributable ("© NCERT / not to be
# republished"). The repo ships the fetch script and derived artifacts, not the book.
ingest/raw/
ingest/pages/
ingest/extracted/
# chapter PDFs pagesource.py fetches on demand — same book, same licence
ingest/pdf_cache/

# secrets — never commit
.env
